/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.unsplash.com", "saglik.anadolusigorta.com.tr"],
  },
};

module.exports = nextConfig;
